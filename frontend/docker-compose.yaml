version: '3'
services:
  react-ui:
    build:
        context: .
        dockerfile: Dockerfile
    environment:
        - CHOKIDAR_USEPOLLING=true
    ports:
      - "3001:3000"
    container_name: react-ui
    stdin_open: true
    volumes:
       - '.:/app'
       - '/app/node_modules'
